<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- LAN API uses http:// — allow cleartext for private ranges and all (local server) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
